# api-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: inventory
spec:
  selector:
    app: inventory-api      # finds pods with this label
  ports:
    - port: 80              # port the Service listens on inside the cluster
      targetPort: 8080      # port container listens on
